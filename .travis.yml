language: python
python:
  - "2.7"
  - "3.2"
  - "3.3"
  - "3.4"
  - "nightly"
  - "pypy"
  - "pypy3"
sudo: false
install: pip install coveralls
script: nosetests --with-coverage --cover-package=pyzufall --with-doctest
after_success: coveralls
after_script: python setup.py install && vcardgen -c 3 test.vcf && cat test.vcf
notifications:
  email:
    - post@davidak.de
